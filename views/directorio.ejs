<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('partials/head-meta') %>
    
    <!-- Estilos específicos del directorio -->
    <style>
        /* ===== DIRECTORIO STYLES ===== */
        .directorio-header {
            background: linear-gradient(135deg, var(--verde-profundo) 0%, var(--verde-hoja) 100%);
            color: white;
            padding: 120px 0 80px;
            position: relative;
            overflow: hidden;
        }
        
        .directorio-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('../assets/images/workshop_tools.jpg') center/cover;
            opacity: 0.1;
            z-index: 1;
        }
        
        .directorio-header .container {
            position: relative;
            z-index: 2;
        }
        
        .directorio-title {
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 700;
            margin-bottom: 1rem;
            text-align: center;
        }
        
        .directorio-subtitle {
            font-size: clamp(1.2rem, 2.5vw, 1.5rem);
            text-align: center;
            margin-bottom: 2rem;
            opacity: 0.9;
        }
        
        .user-info {
            text-align: center;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 12px;
            padding: 1rem 2rem;
            margin-top: 2rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .user-info p {
            margin: 0;
            font-size: 1rem;
            color: rgba(255, 255, 255, 0.9);
        }
        
        /* ===== FILTROS ===== */
        .filtros-section {
            background: white;
            border-bottom: 1px solid #e5e5e5;
            padding: 2rem 0;
            position: sticky;
            top: 80px;
            z-index: 100;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        
        .filtros-container {
            display: flex;
            gap: 1rem;
            align-items: center;
            flex-wrap: wrap;
        }
        
        .filtro-group {
            flex: 1;
            min-width: 200px;
        }
        
        .filtro-label {
            font-weight: 600;
            color: var(--verde-profundo);
            margin-bottom: 0.5rem;
            display: block;
            font-size: 0.9rem;
        }
        
        .filtro-input,
        .filtro-select {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 2px solid #e5e5e5;
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: white;
        }
        
        .filtro-input:focus,
        .filtro-select:focus {
            border-color: var(--verde-profundo);
            outline: none;
            box-shadow: 0 0 0 3px rgba(31, 75, 63, 0.1);
        }
        
        .btn-filtros {
            display: flex;
            gap: 0.5rem;
            align-items: flex-end;
        }
        
        .btn-buscar,
        .btn-limpiar {
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: all 0.3s ease;
            white-space: nowrap;
        }
        
        .btn-buscar {
            background: var(--verde-profundo);
            color: white;
        }
        
        .btn-buscar:hover {
            background: var(--verde-hoja);
            transform: translateY(-2px);
        }
        
        .btn-limpiar {
            background: #f8f9fa;
            color: #666;
            border: 1px solid #ddd;
        }
        
        .btn-limpiar:hover {
            background: #e9ecef;
        }
        
        /* ===== RESULTADOS ===== */
        .resultados-section {
            padding: 3rem 0;
            background: var(--blanco-calido);
        }
        
        .resultados-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }
        
        .resultados-contador {
            font-size: 1.1rem;
            color: var(--verde-profundo);
            font-weight: 600;
        }
        
        .ordenar-select {
            padding: 0.5rem 1rem;
            border: 1px solid #ddd;
            border-radius: 6px;
            background: white;
        }
        
        /* ===== GRID DE PROVEEDORES ===== */
        .proveedores-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(380px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }
        
        .proveedor-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            overflow: hidden;
            transition: all 0.3s ease;
            border: 1px solid rgba(31, 75, 63, 0.1);
            position: relative;
        }
        
        .proveedor-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 40px rgba(0,0,0,0.15);
        }
        
        .proveedor-header {
            padding: 1.5rem;
            border-bottom: 1px solid #f0f0f0;
            position: relative;
        }
        
        .proveedor-logo {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: #f8f9fa;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1rem;
            border: 2px solid #e9ecef;
            font-size: 1.5rem;
        }
        
        .proveedor-nombre {
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--verde-profundo);
            margin-bottom: 0.5rem;
            line-height: 1.3;
        }
        
        .proveedor-categoria {
            background: var(--arena);
            color: var(--verde-profundo);
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
            display: inline-block;
        }
        
        .proveedor-ubicacion {
            color: #666;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-top: 0.5rem;
        }
        
        .proveedor-body {
            padding: 1.5rem;
        }
        
        .proveedor-descripcion {
            color: #555;
            line-height: 1.6;
            margin-bottom: 1.5rem;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
        .proveedor-materiales {
            margin-bottom: 1rem;
        }
        
        .materiales-label {
            font-weight: 600;
            color: var(--verde-profundo);
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
            display: block;
        }
        
        .materiales-list {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
        }
        
        .material-tag {
            background: #f8f9fa;
            color: #495057;
            padding: 0.25rem 0.5rem;
            border-radius: 12px;
            font-size: 0.8rem;
            border: 1px solid #e9ecef;
        }
        
        .proveedor-footer {
            padding: 0 1.5rem 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .certificaciones-mini {
            display: flex;
            gap: 0.25rem;
        }
        
        .cert-badge {
            background: var(--verde-profundo);
            color: white;
            padding: 0.2rem 0.5rem;
            border-radius: 8px;
            font-size: 0.7rem;
            font-weight: 600;
        }
        
        .btn-ver-detalle {
            background: var(--terracota);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }
        
        .btn-ver-detalle:hover {
            background: #B8613A;
            transform: translateY(-2px);
            color: white;
            text-decoration: none;
        }
        
        /* ===== ESTADOS ESPECIALES ===== */
        .loading-state {
            text-align: center;
            padding: 4rem 2rem;
            color: #666;
        }
        
        .loading-spinner {
            border: 3px solid #f3f3f3;
            border-top: 3px solid var(--verde-profundo);
            border-radius: 50%;
            width: 40px;
            height: 40px;
            animation: spin 1s linear infinite;
            margin: 0 auto 1rem;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: #666;
        }
        
        .empty-state-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }
        
        /* ===== RESPONSIVE ===== */
        @media (max-width: 768px) {
            .directorio-header {
                padding: 100px 0 60px;
            }
            
            .filtros-section {
                position: relative;
                top: 0;
            }
            
            .filtros-container {
                flex-direction: column;
                align-items: stretch;
            }
            
            .filtro-group {
                min-width: auto;
            }
            
            .btn-filtros {
                flex-direction: column;
                align-items: stretch;
            }
            
            .resultados-header {
                flex-direction: column;
                gap: 1rem;
                align-items: stretch;
            }
            
            .proveedores-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            
            .user-info {
                margin-top: 1rem;
                padding: 1rem;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>
    
    <!-- Header del Directorio -->
    <header class="directorio-header">
        <div class="container">
            <h1 class="directorio-title">Directorio de Proveedores</h1>
            <p class="directorio-subtitle">
                Proveedores verificados con criterios de sostenibilidad, trazabilidad y compromiso con la economía circular
            </p>
            
            <div class="user-info">
                <p>👋 Bienvenido/a, <strong><%= email %></strong> | Acceso válido hasta <%= new Date(expiresAt).toLocaleDateString('es-ES') %></p>
            </div>
        </div>
    </header>

    <!-- Sección de Filtros -->
    <section class="filtros-section">
        <div class="container">
            <form class="filtros-container" id="filtrosForm">
                <div class="filtro-group">
                    <label class="filtro-label" for="busqueda">🔍 Buscar</label>
                    <input 
                        type="text" 
                        id="busqueda" 
                        name="q" 
                        class="filtro-input" 
                        placeholder="Nombre, materiales, ubicación..."
                        value="<%= filtros.busqueda %>"
                    >
                </div>
                
                <div class="filtro-group">
                    <label class="filtro-label" for="categoria">📂 Categoría</label>
                    <select id="categoria" name="categoria" class="filtro-select">
                        <option value="">Todas las categorías</option>
                        <% categorias.forEach(cat => { %>
                        <option value="<%= cat %>" <%= filtros.categoria === cat ? 'selected' : '' %>><%= cat %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div class="filtro-group">
                    <label class="filtro-label" for="ubicacion">📍 Ubicación</label>
                    <select id="ubicacion" name="ubicacion" class="filtro-select">
                        <option value="">Todas las ubicaciones</option>
                        <% ubicaciones.forEach(ubi => { %>
                        <option value="<%= ubi %>" <%= filtros.ubicacion === ubi ? 'selected' : '' %>><%= ubi %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div class="btn-filtros">
                    <button type="submit" class="btn-buscar">Buscar</button>
                    <button type="button" class="btn-limpiar" onclick="limpiarFiltros()">Limpiar</button>
                </div>
            </form>
        </div>
    </section>

    <!-- Sección de Resultados -->
    <section class="resultados-section">
        <div class="container">
            <div class="resultados-header">
                <div class="resultados-contador">
                    <span id="contadorResultados"><%= proveedores.length %></span> proveedores encontrados
                </div>
                <select class="ordenar-select" id="ordenar" onchange="ordenarResultados()">
                    <option value="nombre">Ordenar por nombre</option>
                    <option value="categoria">Ordenar por categoría</option>
                    <option value="ubicacion">Ordenar por ubicación</option>
                </select>
            </div>

            <!-- Loading State -->
            <div class="loading-state" id="loadingState" style="display: none;">
                <div class="loading-spinner"></div>
                <p>Buscando proveedores...</p>
            </div>

            <!-- Grid de Proveedores -->
            <div class="proveedores-grid" id="proveedoresGrid">
                <% if (proveedores.length === 0) { %>
                <div class="empty-state">
                    <div class="empty-state-icon">🏢</div>
                    <h3>No se encontraron proveedores</h3>
                    <p>Prueba a ajustar tus filtros de búsqueda</p>
                </div>
                <% } else { %>
                    <% proveedores.forEach(proveedor => { %>
                    <article class="proveedor-card">
                        <div class="proveedor-header">
                            <div class="proveedor-logo">
                                <% if (proveedor.logoUrl) { %>
                                    <img src="<%= proveedor.logoUrl %>" alt="<%= proveedor.nombre %>" style="width:100%;height:100%;object-fit:cover;border-radius:50%;">
                                <% } else { %>
                                    🏢
                                <% } %>
                            </div>
                            <h3 class="proveedor-nombre"><%= proveedor.nombre %></h3>
                            <span class="proveedor-categoria"><%= proveedor.categoria %></span>
                            <div class="proveedor-ubicacion">
                                📍 <%= proveedor.ubicacionCorta %>
                            </div>
                        </div>
                        
                        <div class="proveedor-body">
                            <p class="proveedor-descripcion"><%= proveedor.descripcion %></p>
                            
                            <div class="proveedor-materiales">
                                <span class="materiales-label">Materiales:</span>
                                <div class="materiales-list">
                                    <% proveedor.materiales.slice(0, 3).forEach(material => { %>
                                    <span class="material-tag"><%= material %></span>
                                    <% }) %>
                                    <% if (proveedor.materiales.length > 3) { %>
                                    <span class="material-tag">+<%= proveedor.materiales.length - 3 %> más</span>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                        
                        <div class="proveedor-footer">
                            <div class="certificaciones-mini">
                                <% proveedor.certificaciones.slice(0, 2).forEach(cert => { %>
                                <span class="cert-badge"><%= cert %></span>
                                <% }) %>
                            </div>
                            <a href="/dir/proveedor/<%= proveedor.id %>?token=<%= q.token %>" class="btn-ver-detalle">
                                Ver detalles
                            </a>
                        </div>
                    </article>
                    <% }) %>
                <% } %>
            </div>
        </div>
    </section>

    <%- include('partials/footer') %>

    <!-- JavaScript para filtros y búsqueda -->
    <script>
        // Configuración
        const currentToken = '<%= q.token %>';
        
        // Elementos del DOM
        const filtrosForm = document.getElementById('filtrosForm');
        const loadingState = document.getElementById('loadingState');
        const proveedoresGrid = document.getElementById('proveedoresGrid');
        const contadorResultados = document.getElementById('contadorResultados');
        
        // Event Listeners
        filtrosForm.addEventListener('submit', handleBusqueda);
        
        // Función para manejar la búsqueda
        async function handleBusqueda(e) {
            e.preventDefault();
            
            const formData = new FormData(filtrosForm);
            const params = new URLSearchParams();
            
            for (let [key, value] of formData.entries()) {
                if (value.trim()) {
                    params.append(key, value);
                }
            }
            
            try {
                mostrarLoading(true);
                
                const response = await fetch(`/api/dir/search?${params}`);
                const data = await response.json();
                
                if (data.success) {
                    renderProveedores(data.proveedores);
                    contadorResultados.textContent = data.total;
                } else {
                    console.error('Error en búsqueda:', data.error);
                }
                
            } catch (error) {
                console.error('Error realizando búsqueda:', error);
            } finally {
                mostrarLoading(false);
            }
        }
        
        // Función para limpiar filtros
        function limpiarFiltros() {
            filtrosForm.reset();
            handleBusqueda({ preventDefault: () => {} });
        }
        
        // Función para mostrar/ocultar loading
        function mostrarLoading(show) {
            loadingState.style.display = show ? 'block' : 'none';
            proveedoresGrid.style.display = show ? 'none' : 'grid';
        }
        
        // Función para renderizar proveedores
        function renderProveedores(proveedores) {
            if (proveedores.length === 0) {
                proveedoresGrid.innerHTML = `
                    <div class="empty-state">
                        <div class="empty-state-icon">🔍</div>
                        <h3>No se encontraron proveedores</h3>
                        <p>Prueba a ajustar tus filtros de búsqueda</p>
                    </div>
                `;
                return;
            }
            
            proveedoresGrid.innerHTML = proveedores.map(proveedor => `
                <article class="proveedor-card">
                    <div class="proveedor-header">
                        <div class="proveedor-logo">
                            ${proveedor.logoUrl ? 
                                `<img src="${proveedor.logoUrl}" alt="${proveedor.nombre}" style="width:100%;height:100%;object-fit:cover;border-radius:50%;">` : 
                                '🏢'
                            }
                        </div>
                        <h3 class="proveedor-nombre">${proveedor.nombre}</h3>
                        <span class="proveedor-categoria">${proveedor.categoria}</span>
                        <div class="proveedor-ubicacion">📍 ${proveedor.ubicacionCorta}</div>
                    </div>
                    
                    <div class="proveedor-body">
                        <p class="proveedor-descripcion">${proveedor.descripcion}</p>
                        
                        <div class="proveedor-materiales">
                            <span class="materiales-label">Materiales:</span>
                            <div class="materiales-list">
                                ${proveedor.materiales.slice(0, 3).map(material => 
                                    `<span class="material-tag">${material}</span>`
                                ).join('')}
                                ${proveedor.materiales.length > 3 ? 
                                    `<span class="material-tag">+${proveedor.materiales.length - 3} más</span>` : 
                                    ''
                                }
                            </div>
                        </div>
                    </div>
                    
                    <div class="proveedor-footer">
                        <div class="certificaciones-mini">
                            ${proveedor.certificaciones.slice(0, 2).map(cert => 
                                `<span class="cert-badge">${cert}</span>`
                            ).join('')}
                        </div>
                        <a href="/dir/proveedor/${proveedor.id}?token=${currentToken}" class="btn-ver-detalle">
                            Ver detalles
                        </a>
                    </div>
                </article>
            `).join('');
        }
        
        // Función para ordenar resultados
        function ordenarResultados() {
            const ordenar = document.getElementById('ordenar').value;
            // Por ahora solo actualizamos la URL, la ordenación se puede implementar después
            console.log('Ordenar por:', ordenar);
        }

        // Analytics tracking
        window.dataLayer = window.dataLayer || [];
        window.dataLayer.push({ 
            event: 'directorio_access',
            user_email: '<%= email %>',
            access_type: 'valid_token'
        });
    </script>
</body>
</html>