<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('partials/head-meta') %>
    
    <!-- Estilos específicos del detalle de proveedor -->
    <style>
        /* ===== DETALLE PROVEEDOR STYLES ===== */
        .proveedor-hero {
            background: var(--surface, #fbf9f8);
            color: var(--primary, #023429);
            padding: 100px 0 60px;
            position: relative;
            overflow: hidden;
        }

        .proveedor-hero::before {
            display: none;
        }
        
        .proveedor-hero .container {
            position: relative;
            z-index: 2;
        }
        
        .breadcrumbs {
            margin-bottom: 2rem;
            font-size: 0.9rem;
        }
        
        .breadcrumbs a {
            color: var(--primary, #023429);
            opacity: 0.5;
            text-decoration: none;
            transition: opacity 0.2s ease;
        }

        .breadcrumbs a:hover {
            opacity: 0.8;
        }

        .breadcrumbs span {
            color: var(--primary, #023429);
            opacity: 0.3;
            margin: 0 0.5rem;
        }
        
        .proveedor-hero-content {
            display: grid;
            grid-template-columns: auto 1fr;
            gap: 2rem;
            align-items: start;
        }
        
        .proveedor-logo-hero {
            width: 120px;
            height: 120px;
            border-radius: 16px;
            background: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            box-shadow: 0 2px 16px rgba(31, 75, 63, 0.08);
            border: 1.5px solid rgba(31, 75, 63, 0.08);
        }
        
        .proveedor-info-hero {
            min-width: 0;
        }
        
        .proveedor-titulo {
            font-family: var(--font-headline, 'Newsreader', serif);
            font-size: clamp(2rem, 4vw, 2.8rem);
            font-weight: 700;
            margin-bottom: 0.5rem;
            line-height: 1.15;
            color: var(--primary, #023429);
        }
        
        .proveedor-categoria-hero {
            background: var(--secondary-container, #fd9f6a);
            color: var(--primary, #023429);
            padding: 0.4rem 1rem;
            border-radius: 25px;
            font-family: var(--font-body, 'Manrope', sans-serif);
            font-size: 0.85rem;
            font-weight: 600;
            display: inline-block;
            margin-bottom: 1rem;
        }
        
        .proveedor-ubicacion-hero {
            font-size: 1.2rem;
            opacity: 0.9;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 1.5rem;
        }
        
        .proveedor-descripcion-hero {
            font-family: var(--font-body, 'Manrope', sans-serif);
            font-size: 1.05rem;
            line-height: 1.6;
            opacity: 0.7;
            max-width: 700px;
        }
        
        /* ===== SECCIÓN PRINCIPAL ===== */
        .proveedor-main {
            padding: 4rem 0;
            background: var(--surface, #fbf9f8);
        }
        
        .proveedor-grid {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 4rem;
            align-items: start;
        }
        
        .proveedor-content {
            background: white;
            border-radius: 16px;
            padding: 2.5rem;
            box-shadow: 0 2px 16px rgba(31, 75, 63, 0.06);
        }
        
        .proveedor-sidebar {
            background: white;
            border-radius: 16px;
            padding: 2rem;
            box-shadow: 0 2px 16px rgba(31, 75, 63, 0.06);
            position: sticky;
            top: 100px;
        }
        
        .content-section {
            margin-bottom: 3rem;
        }
        
        .content-section:last-child {
            margin-bottom: 0;
        }
        
        .section-title {
            font-family: var(--font-headline, 'Newsreader', serif);
            font-size: 1.35rem;
            font-weight: 600;
            color: var(--primary, #023429);
            margin-bottom: 1.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 1.5px solid rgba(31, 75, 63, 0.1);
        }
        
        /* ===== MATERIALES SECTION ===== */
        .materiales-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 1rem;
        }
        
        .material-card {
            background: var(--surface, #fbf9f8);
            border: none;
            border-radius: 12px;
            padding: 1rem;
            text-align: center;
            transition: all 0.25s ease;
        }

        .material-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 16px rgba(31, 75, 63, 0.08);
        }
        
        .material-icon {
            font-size: 2rem;
            margin-bottom: 0.5rem;
            display: block;
        }
        
        .material-nombre {
            font-family: var(--font-body, 'Manrope', sans-serif);
            font-weight: 600;
            color: var(--primary, #023429);
        }
        
        /* ===== CERTIFICACIONES SECTION ===== */
        .certificaciones-list {
            display: flex;
            flex-wrap: wrap;
            gap: 1rem;
        }
        
        .cert-card {
            background: var(--primary, #023429);
            color: white;
            padding: 0.75rem 1.25rem;
            border-radius: 8px;
            font-family: var(--font-body, 'Manrope', sans-serif);
            font-weight: 600;
            font-size: 0.9rem;
            text-align: center;
            min-width: 120px;
            transition: all 0.25s ease;
        }

        .cert-card:hover {
            background: var(--primary-container, #1F4B3F);
            transform: translateY(-2px);
        }
        
        /* ===== SIDEBAR STYLES ===== */
        .sidebar-section {
            margin-bottom: 2rem;
            padding-bottom: 2rem;
            border-bottom: 1px solid #e9ecef;
        }
        
        .sidebar-section:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
            border-bottom: none;
        }
        
        .sidebar-title {
            font-family: var(--font-headline, 'Newsreader', serif);
            font-size: 1.15rem;
            font-weight: 600;
            color: var(--primary, #023429);
            margin-bottom: 1rem;
        }
        
        .contact-info {
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
        }
        
        .contact-item {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.5rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }
        
        .contact-item:hover {
            background: #f8f9fa;
        }
        
        .contact-icon {
            width: 40px;
            height: 40px;
            background: var(--surface, #fbf9f8);
            color: var(--primary, #023429);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.1rem;
            flex-shrink: 0;
        }
        
        .contact-text {
            min-width: 0;
        }
        
        .contact-label {
            font-size: 0.8rem;
            color: #666;
            display: block;
        }
        
        .contact-value {
            font-weight: 600;
            color: var(--primary, #023429);
            word-break: break-all;
        }
        
        .contact-value a {
            color: inherit;
            text-decoration: none;
        }
        
        .contact-value a:hover {
            color: var(--secondary, #934a1c);
        }
        
        /* ===== BOTONES DE ACCIÓN ===== */
        .actions-section {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
        
        .btn-action {
            background: var(--primary, #023429);
            color: white;
            padding: 0.85rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            font-family: var(--font-body, 'Manrope', sans-serif);
            font-weight: 600;
            text-align: center;
            transition: all 0.25s ease;
            border: none;
            cursor: pointer;
            font-size: 0.95rem;
        }

        .btn-action:hover {
            background: var(--primary-container, #1F4B3F);
            transform: translateY(-2px);
            color: white;
            text-decoration: none;
        }

        .btn-secondary {
            background: transparent;
            color: var(--primary, #023429);
            border: 1.5px solid rgba(31, 75, 63, 0.2);
        }

        .btn-secondary:hover {
            background: var(--secondary-container, #fd9f6a);
            color: var(--primary, #023429);
            border-color: var(--secondary-container, #fd9f6a);
        }
        
        /* ===== TAGS SECTION ===== */
        .tags-list {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
        }
        
        .tag {
            background: var(--surface, #fbf9f8);
            color: var(--primary, #023429);
            padding: 0.3rem 0.75rem;
            border-radius: 20px;
            font-family: var(--font-body, 'Manrope', sans-serif);
            font-size: 0.8rem;
            font-weight: 500;
            border: 1px solid rgba(31, 75, 63, 0.1);
        }
        
        /* ===== VERIFICACIÓN BADGE ===== */
        .verificacion-badge {
            background: linear-gradient(135deg, #10b981, #059669);
            color: white;
            padding: 0.75rem 1rem;
            border-radius: 12px;
            text-align: center;
            font-weight: 600;
            margin-bottom: 1rem;
            position: relative;
            overflow: hidden;
        }
        
        .verificacion-badge::before {
            content: '✓';
            position: absolute;
            top: 0.5rem;
            right: 1rem;
            font-size: 1.5rem;
            opacity: 0.3;
        }
        
        .verificacion-text {
            font-size: 0.9rem;
            margin: 0;
        }
        
        .verificacion-fecha {
            font-size: 0.8rem;
            opacity: 0.9;
            margin-top: 0.25rem;
        }
        
        /* ===== RESPONSIVE ===== */
        @media (max-width: 968px) {
            .proveedor-grid {
                grid-template-columns: 1fr;
                gap: 2rem;
            }
            
            .proveedor-sidebar {
                position: relative;
                top: 0;
                order: -1;
            }
        }
        
        @media (max-width: 768px) {
            .proveedor-hero {
                padding: 100px 0 40px;
            }
            
            .proveedor-hero-content {
                grid-template-columns: 1fr;
                gap: 1.5rem;
                text-align: center;
            }
            
            .proveedor-logo-hero {
                width: 100px;
                height: 100px;
                justify-self: center;
            }
            
            .proveedor-main {
                padding: 2rem 0;
            }
            
            .proveedor-content,
            .proveedor-sidebar {
                padding: 1.5rem;
            }
            
            .materiales-grid {
                grid-template-columns: 1fr;
            }
            
            .certificaciones-list {
                flex-direction: column;
            }
            
            .cert-card {
                min-width: auto;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>
    
    <!-- Hero del Proveedor -->
    <header class="proveedor-hero">
        <div class="container">
            <!-- Breadcrumbs -->
            <nav class="breadcrumbs">
                <a href="/dir?token=<%= q.token %>">← Directorio</a>
                <span>/</span>
                <span><%= proveedor.nombre %></span>
            </nav>
            
            <div class="proveedor-hero-content">
                <div class="proveedor-logo-hero">
                    <% if (proveedor.logoUrl) { %>
                        <img src="<%= proveedor.logoUrl %>" alt="<%= proveedor.nombre %>" style="width:100%;height:100%;object-fit:cover;border-radius:13px;">
                    <% } else { %>
                        🏢
                    <% } %>
                </div>
                
                <div class="proveedor-info-hero">
                    <h1 class="proveedor-titulo"><%= proveedor.nombre %></h1>
                    <span class="proveedor-categoria-hero"><%= proveedor.categoria %></span>
                    <div class="proveedor-ubicacion-hero">
                        📍 <%= proveedor.ubicacion %>
                    </div>
                    <p class="proveedor-descripcion-hero">
                        <%= proveedor.descripcion %>
                    </p>
                </div>
            </div>
        </div>
    </header>

    <!-- Contenido Principal -->
    <main class="proveedor-main">
        <div class="container">
            <div class="proveedor-grid">
                <!-- Contenido Principal -->
                <div class="proveedor-content">
                    <!-- Materiales -->
                    <% if (proveedor.materiales && proveedor.materiales.length > 0) { %>
                    <section class="content-section">
                        <h2 class="section-title">🧱 Materiales y Especialidades</h2>
                        <div class="materiales-grid">
                            <% proveedor.materiales.forEach(material => { %>
                            <div class="material-card">
                                <span class="material-icon">🔸</span>
                                <div class="material-nombre"><%= material %></div>
                            </div>
                            <% }) %>
                        </div>
                    </section>
                    <% } %>
                    
                    <!-- Certificaciones -->
                    <% if (proveedor.certificaciones && proveedor.certificaciones.length > 0) { %>
                    <section class="content-section">
                        <h2 class="section-title">🏆 Certificaciones y Estándares</h2>
                        <div class="certificaciones-list">
                            <% proveedor.certificaciones.forEach(cert => { %>
                            <div class="cert-card">
                                <%= cert %>
                            </div>
                            <% }) %>
                        </div>
                    </section>
                    <% } %>
                    
                    <!-- Información Adicional -->
                    <% if (proveedor.notas) { %>
                    <section class="content-section">
                        <h2 class="section-title">📋 Información Adicional</h2>
                        <div class="info-adicional">
                            <p><%= proveedor.notas %></p>
                        </div>
                    </section>
                    <% } %>
                </div>
                
                <!-- Sidebar -->
                <aside class="proveedor-sidebar">
                    <!-- Badge de Verificación -->
                    <div class="verificacion-badge">
                        <div class="verificacion-text">✅ Proveedor Verificado</div>
                        <% if (proveedor.fechaVerificacion) { %>
                        <div class="verificacion-fecha">Verificado el <%= new Date(proveedor.fechaVerificacion).toLocaleDateString('es-ES') %></div>
                        <% } %>
                    </div>
                    
                    <!-- Información de Contacto -->
                    <div class="sidebar-section">
                        <h3 class="sidebar-title">📞 Contacto</h3>
                        <div class="contact-info">
                            <% if (proveedor.email) { %>
                            <div class="contact-item">
                                <div class="contact-icon">📧</div>
                                <div class="contact-text">
                                    <span class="contact-label">Email</span>
                                    <div class="contact-value">
                                        <a href="mailto:<%= proveedor.email %>"><%= proveedor.email %></a>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            
                            <% if (proveedor.telefono) { %>
                            <div class="contact-item">
                                <div class="contact-icon">📱</div>
                                <div class="contact-text">
                                    <span class="contact-label">Teléfono</span>
                                    <div class="contact-value">
                                        <a href="tel:<%= proveedor.telefono %>"><%= proveedor.telefono %></a>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                            
                            <% if (proveedor.web) { %>
                            <div class="contact-item">
                                <div class="contact-icon">🌐</div>
                                <div class="contact-text">
                                    <span class="contact-label">Sitio web</span>
                                    <div class="contact-value">
                                        <a href="<%= proveedor.web %>" target="_blank" rel="noopener noreferrer">Visitar web</a>
                                    </div>
                                </div>
                            </div>
                            <% } %>
                        </div>
                    </div>
                    
                    <!-- Tags -->
                    <% if (proveedor.tags && proveedor.tags.length > 0) { %>
                    <div class="sidebar-section">
                        <h3 class="sidebar-title">🏷️ Características</h3>
                        <div class="tags-list">
                            <% proveedor.tags.forEach(tag => { %>
                            <span class="tag"><%= tag %></span>
                            <% }) %>
                        </div>
                    </div>
                    <% } %>
                    
                    <!-- Acciones -->
                    <div class="sidebar-section">
                        <h3 class="sidebar-title">🚀 Acciones</h3>
                        <div class="actions-section">
                            <% if (proveedor.email) { %>
                            <a href="mailto:<%= proveedor.email %>?subject=Consulta desde Oficios Circulares" class="btn-action">
                                ✉️ Contactar ahora
                            </a>
                            <% } %>
                            <% if (proveedor.web) { %>
                            <a href="<%= proveedor.web %>" target="_blank" rel="noopener noreferrer" class="btn-action btn-secondary">
                                🌐 Visitar web
                            </a>
                            <% } %>
                            <a href="/dir?token=<%= q.token %>" class="btn-action btn-secondary">
                                ← Volver al directorio
                            </a>
                        </div>
                    </div>
                </aside>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>

    <script>
        // Analytics tracking
        window.dataLayer = window.dataLayer || [];
        window.dataLayer.push({ 
            event: 'proveedor_detail_view',
            user_email: '<%= email %>',
            proveedor_id: '<%= proveedor.id %>',
            proveedor_nombre: '<%= proveedor.nombre %>',
            proveedor_categoria: '<%= proveedor.categoria %>'
        });
    </script>
</body>
</html>